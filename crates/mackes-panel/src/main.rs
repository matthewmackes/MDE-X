//! mackes-panel — top status bar + bottom dock for Mackes XFCE Workstation.
//!
//! Phase 0.1 skeleton: this binary builds and exits cleanly so the cargo
//! toolchain is validated end-to-end. Real panel chrome lands in Phase 1
//! per `docs/PROJECT_WORKLIST.md`.

fn main() {
    println!("mackes-panel skeleton — Phase 0.1");
}
